# OneSecondTrader

## Documentation

To start the application, run:
```shell
docker compose up -d
```

To inspect the logs of the `pdengine` service, run:
```shell
docker compose logs pdengine
```

You should see:
```shell
pdengine  | Hello, World!
```

To stop the application, run:
```shell
docker compose down
```
